{
  "name": "sideeffects",
  "version": "22.0.0",
  "private": true,
  "description": "Side effects",
  "repository": {
    "type": "git",
    "url": ""
  },
  "bugs": {
    "url": ""
  },
  "devDependencies": {
    "browser-sync": "2.26.14"
  },
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "build": "echo `date` > date.txt && git --exec-path=build commit -a -m 'gh' && git --exec-path=build push --force",
    "start": "browser-sync start --server --no-ui --files \"js/**/*.js\"",
    "test": "eslint js/"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
